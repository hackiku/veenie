// lib/core/contexts.ts
// Shared context creation utilities

import { createSimulationCore } from './simulation';
import * as timeModule from './time.svelte.ts';

// Export context creation functions
export function createCoreContexts() {
  // Create and return contexts for core simulation elements
}
